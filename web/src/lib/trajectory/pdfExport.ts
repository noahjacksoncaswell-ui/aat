import { jsPDF } from "jspdf";
import autoTable from "jspdf-autotable";
import { enuToLatLon } from "./geo";
import { PHASE_LABELS, EVENT_LABELS } from "./labels";
import type { RunMeta } from "./formConfig";
import type { SimConfig, SimResult } from "./types";

// v6.0 Section 6.5 - Export Simulation. A single, coherent PDF technical
// report (the agent's discretion on exact format, per the directive)
// containing the full resolved input parameter set, the complete
// time-series dataset, all graphs as embedded images, the phase/event/
// landing tables, and the 3D trajectory view. Courier substitutes for the
// app's IBM Plex Mono web font here (jsPDF's built-in fonts are Helvetica/
// Times/Courier; embedding a custom font adds a base64 VFS step with no
// functional payoff for a generated report) - Courier is the closest
// built-in match to a technical monospace aesthetic.

const GOVERNING_DISCLAIMER =
  "This report is generated by a simplified point-mass trajectory simulation. It is explicitly NOT a 6-degree-of-freedom (6-DOF) flight dynamics simulation: it models no vehicle attitude, no rotational dynamics, no stability margins, and no tumbling. It provides an illustrative estimate of where the vehicle goes, not whether the vehicle remains aerodynamically stable getting there. This tool is a planning/illustration aid; its output must never be presented as, or mistaken for, a certified range safety analysis.";

const CLASSIFICATION_LINE = "ITAR-CONTROLLED // DISTRIBUTION IS LIMITED — U.S. PERSONS ONLY — SEE 22 CFR PARTS 120-130";

function imageFormat(dataUrl: string): "PNG" | "JPEG" {
  return dataUrl.startsWith("data:image/jpeg") ? "JPEG" : "PNG";
}

export function formatRunTimestamp(iso: string): string {
  const d = new Date(iso);
  const yyyy = d.getUTCFullYear();
  const mm = String(d.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(d.getUTCDate()).padStart(2, "0");
  const hh = String(d.getUTCHours()).padStart(2, "0");
  const min = String(d.getUTCMinutes()).padStart(2, "0");
  return `${yyyy}-${mm}-${dd} ${hh}${min}z`;
}

export function reportTitle(meta: RunMeta): string {
  return `Trajectory Simulation — ${meta.populatedVehicleName ?? "Unspecified Vehicle"} — ${meta.siteDesignator} — ${formatRunTimestamp(meta.runAt)}`;
}

function descentDescription(config: SimConfig): { label: string; rows: [string, string][] } {
  const d = config.descent;
  switch (d.kind) {
    case "A":
      return { label: "A. Main Chute Only", rows: [["Main Chute Cd", String(d.mainCd)], ["Main Chute Reference Area (m²)", String(d.mainArea)]] };
    case "B":
      return {
        label: "B. Dual-Deploy",
        rows: [
          ["Drogue Cd", String(d.drogueCd)],
          ["Drogue Reference Area (m²)", String(d.drogueArea)],
          ["Main Deploy Altitude (m AGL)", String(d.mainDeployAltitudeM)],
          ["Main Chute Cd", String(d.mainCd)],
          ["Main Chute Reference Area (m²)", String(d.mainArea)],
        ],
      };
    case "C":
      return {
        label: "C. Guided Descent (Airbrake/Fin-Modulated)",
        rows: [
          ["Guided-Descent Cd", String(d.guidedCd)],
          ["Guided-Descent Reference Area (m²)", String(d.guidedArea)],
          ["Main Deploy Altitude (m AGL)", String(d.mainDeployAltitudeM)],
          ["Main Chute Cd", String(d.mainCd)],
          ["Main Chute Reference Area (m²)", String(d.mainArea)],
        ],
      };
    case "D":
      return { label: "D. Ballistic (No Recovery Deployment)", rows: [] };
    case "E":
      return { label: "E. Custom Descent Profile", rows: [["Custom Descent Cd", String(d.customCd)], ["Custom Descent Reference Area (m²)", String(d.customArea)]] };
  }
}

export interface ChartImage {
  title: string;
  dataUrl: string;
}

export function buildTrajectoryReportPdf(args: {
  config: SimConfig;
  meta: RunMeta;
  result: SimResult;
  chartImages: ChartImage[];
  scene3dDataUrl: string | null;
}): jsPDF {
  const { config, meta, result, chartImages, scene3dDataUrl } = args;
  // compress: true enables PDF stream compression - without it, the raw
  // data appendix's tens of thousands of autotable cell-drawing operators
  // (thousands of rows x 12 columns) bloat the file dramatically.
  const doc = new jsPDF({ unit: "pt", format: "letter", compress: true });
  const pageWidth = doc.internal.pageSize.getWidth();
  const margin = 40;

  function header(title: string) {
    doc.setFont("helvetica", "bold");
    doc.setFontSize(9);
    doc.setTextColor(120, 120, 120);
    doc.text("AAT LOD PLATFORM — TRAJECTORY SIMULATION REPORT", margin, 24);
    doc.text(title, pageWidth - margin, 24, { align: "right" });
    doc.setDrawColor(60, 60, 60);
    doc.line(margin, 30, pageWidth - margin, 30);
  }

  // --- Title page ---
  doc.setFont("helvetica", "bold");
  doc.setFontSize(20);
  doc.setTextColor(20, 20, 20);
  doc.text("TRAJECTORY SIMULATION REPORT", margin, 90);

  doc.setFontSize(11);
  doc.setFont("courier", "normal");
  doc.setTextColor(60, 60, 60);
  doc.text(
    [
      `Vehicle: ${meta.populatedVehicleName ?? "Unspecified Vehicle"}`,
      `Launch Site: ${meta.siteName} (${meta.siteDesignator})`,
      `Run Date/Time: ${formatRunTimestamp(meta.runAt)}`,
      `Map Type: ${meta.mapType}`,
    ],
    margin,
    130
  );

  doc.setFont("helvetica", "bold");
  doc.setFontSize(10);
  doc.setTextColor(180, 120, 20);
  doc.text("SCOPE OF THIS SIMULATION", margin, 210);
  doc.setFont("courier", "normal");
  doc.setFontSize(9);
  doc.setTextColor(80, 80, 80);
  const disclaimerLines = doc.splitTextToSize(GOVERNING_DISCLAIMER, pageWidth - margin * 2);
  doc.text(disclaimerLines, margin, 226);

  doc.setFont("helvetica", "bold");
  doc.setFontSize(8);
  doc.setTextColor(140, 60, 40);
  doc.text(CLASSIFICATION_LINE, pageWidth / 2, doc.internal.pageSize.getHeight() - 24, { align: "center" });

  // --- Input Parameters ---
  doc.addPage();
  header(reportTitle(meta));
  const descent = descentDescription(config);
  autoTable(doc, {
    startY: 44,
    head: [["Input Parameters", ""]],
    body: [
      ["Populated From Vehicle", meta.populatedVehicleName ?? "None (manually entered)"],
      ["Wet Mass (kg)", config.vehicle.wetMassKg.toFixed(3)],
      ["Dry Mass (kg)", config.vehicle.dryMassKg.toFixed(3)],
      ["Total Impulse (N·s)", config.vehicle.totalImpulseNs.toFixed(1)],
      ["Burn Time (s)", config.vehicle.burnTimeS.toFixed(2)],
      ["Reference Diameter (m)", config.vehicle.referenceDiameterM.toFixed(4)],
      ["Ascent Drag Coefficient (Cd)", config.vehicle.dragCoefficient.toFixed(3)],
      ["Launch Rail/Rod Angle (deg from vertical)", config.vehicle.railAngleDeg.toFixed(1)],
      ["Launch Azimuth (deg from true north)", config.vehicle.launchAzimuthDeg.toFixed(1)],
      ["Descent Configuration", descent.label],
      ...descent.rows,
      ["Surface Temperature (K)", config.atmosphere.surfaceTempK.toFixed(2)],
      ["Surface Pressure (Pa)", config.atmosphere.surfacePressurePa.toFixed(1)],
      ["Site Elevation (m)", config.atmosphere.siteElevationM.toFixed(1)],
      ...config.windBands.map((w) => [`Wind @ ${w.altitudeM.toFixed(0)}m AGL`, `${w.speedKts.toFixed(1)} kts from ${w.directionFromDeg.toFixed(0)}°`] as [string, string]),
      ["Launch Site", `${meta.siteName} (${meta.siteDesignator})`],
      ["Launch Coordinates", `${meta.origin.lat.toFixed(5)}, ${meta.origin.lon.toFixed(5)}`],
      ["Active COA On File", meta.activeCoa ? "Yes" : "No"],
      ...(meta.activeCoa
        ? ([
            ["COA Authorized Operation Radius (nm)", meta.activeCoa.authorizedOperationRadiusNm.toFixed(2)],
            ["COA Altitude Limit", meta.activeCoa.altitudeLimits],
          ] as [string, string][])
        : []),
      ["Map Type", meta.mapType],
      ["Timestep (s)", config.timestepS.toFixed(3)],
    ],
    styles: { font: "courier", fontSize: 8, textColor: [30, 30, 30] },
    headStyles: { font: "helvetica", fontStyle: "bold", fillColor: [20, 20, 20], textColor: [255, 255, 255] },
    margin: { left: margin, right: margin },
  });

  // --- Summary Results ---
  const touchdown = result.timeSeries[result.timeSeries.length - 1];
  const { lat, lon } = enuToLatLon(meta.origin.lat, meta.origin.lon, touchdown.xEastM, touchdown.yNorthM);
  const straightLineM = Math.hypot(touchdown.xEastM, touchdown.yNorthM);
  const coaRadiusM = meta.activeCoa ? meta.activeCoa.authorizedOperationRadiusNm * 1852 : null;
  const withinCoa = coaRadiusM != null ? straightLineM <= coaRadiusM : null;
  const maxVelocity = Math.max(...result.timeSeries.map((p) => p.speedMs));
  const maxMach = Math.max(...result.timeSeries.map((p) => p.mach));

  doc.addPage();
  header(reportTitle(meta));
  autoTable(doc, {
    startY: 44,
    head: [["Summary Results", ""]],
    body: [
      ["Apogee Altitude (m)", result.apogeeAltitudeM.toFixed(1)],
      ["Apogee Time (s MET)", result.apogeeTimeS.toFixed(2)],
      ["Max Velocity (m/s)", maxVelocity.toFixed(1)],
      ["Max Mach", maxMach.toFixed(3)],
      ["Flight Duration (s)", touchdown.tS.toFixed(2)],
      ["Touchdown Coordinates", `${lat.toFixed(5)}, ${lon.toFixed(5)}`],
      ["Touchdown Downrange Distance (m)", touchdown.downrangeM.toFixed(1)],
      ["Touchdown Crossrange Drift (m)", touchdown.crossrangeM.toFixed(1)],
      ["Straight-Line Distance from Launch Site (m)", straightLineM.toFixed(1)],
      ["Within COA Authorized Operation Radius", withinCoa == null ? "N/A — No Active COA On File" : withinCoa ? "Yes" : "No"],
    ],
    styles: { font: "courier", fontSize: 8, textColor: [30, 30, 30] },
    headStyles: { font: "helvetica", fontStyle: "bold", fillColor: [20, 20, 20], textColor: [255, 255, 255] },
    margin: { left: margin, right: margin },
  });

  autoTable(doc, {
    startY: (doc as any).lastAutoTable.finalY + 20,
    head: [["Phase", "Start (s)", "End (s)", "Duration (s)", "Start Alt (m)", "End Alt (m)", "Max Velocity (m/s)"]],
    body: result.phaseSummaries.map((p) => [
      PHASE_LABELS[p.phase],
      p.startS.toFixed(2),
      p.endS.toFixed(2),
      p.durationS.toFixed(2),
      p.startAltitudeM.toFixed(1),
      p.endAltitudeM.toFixed(1),
      p.maxVelocityMs.toFixed(1),
    ]),
    styles: { font: "courier", fontSize: 8 },
    headStyles: { font: "helvetica", fontStyle: "bold", fillColor: [20, 20, 20], textColor: [255, 255, 255] },
    margin: { left: margin, right: margin },
  });

  autoTable(doc, {
    startY: (doc as any).lastAutoTable.finalY + 20,
    head: [["Event", "Time (s)", "Altitude (m)", "Velocity (m/s)", "Downrange (m)"]],
    body: result.events.map((e) => [EVENT_LABELS[e.name], e.tS.toFixed(2), e.altitudeM.toFixed(1), e.speedMs.toFixed(1), e.downrangeM.toFixed(1)]),
    styles: { font: "courier", fontSize: 8 },
    headStyles: { font: "helvetica", fontStyle: "bold", fillColor: [20, 20, 20], textColor: [255, 255, 255] },
    margin: { left: margin, right: margin },
  });

  // --- 3D Visualization snapshot ---
  if (scene3dDataUrl) {
    doc.addPage();
    header(reportTitle(meta));
    doc.setFont("helvetica", "bold");
    doc.setFontSize(11);
    doc.setTextColor(20, 20, 20);
    doc.text("3D Visualization", margin, 54);
    const imgW = pageWidth - margin * 2;
    const imgH = imgW * 0.6;
    doc.addImage(scene3dDataUrl, imageFormat(scene3dDataUrl), margin, 66, imgW, imgH);
  }

  // --- Graphs, two per page ---
  for (let i = 0; i < chartImages.length; i += 2) {
    doc.addPage();
    header(reportTitle(meta));
    const imgW = pageWidth - margin * 2;
    const imgH = imgW * 0.42;
    const first = chartImages[i];
    doc.setFont("helvetica", "bold");
    doc.setFontSize(10);
    doc.setTextColor(20, 20, 20);
    doc.text(first.title, margin, 50);
    doc.addImage(first.dataUrl, imageFormat(first.dataUrl), margin, 58, imgW, imgH);

    const second = chartImages[i + 1];
    if (second) {
      const y2 = 58 + imgH + 28;
      doc.text(second.title, margin, y2 - 8);
      doc.addImage(second.dataUrl, imageFormat(second.dataUrl), margin, y2, imgW, imgH);
    }
  }

  // --- Predicted Landing ---
  doc.addPage();
  header(reportTitle(meta));
  doc.setFont("helvetica", "bold");
  doc.setFontSize(11);
  doc.text("Predicted Landing", margin, 50);
  autoTable(doc, {
    startY: 60,
    body: [
      ["Touchdown Coordinates", `${lat.toFixed(5)}, ${lon.toFixed(5)}`],
      ["Downrange Distance (m)", touchdown.downrangeM.toFixed(1)],
      ["Crossrange Drift (m)", touchdown.crossrangeM.toFixed(1)],
      ["Straight-Line Distance from Launch Site (m)", straightLineM.toFixed(1)],
      ["Within COA Authorized Operation Radius", withinCoa == null ? "N/A — No Active COA On File" : withinCoa ? "Yes" : "No"],
    ],
    styles: { font: "courier", fontSize: 8 },
    margin: { left: margin, right: margin },
  });

  // --- Raw Data appendix (Section 5.3 - complete time-series) ---
  doc.addPage();
  header(reportTitle(meta));
  doc.setFont("helvetica", "bold");
  doc.setFontSize(11);
  doc.text(`Raw Data Appendix (${result.timeSeries.length} samples, complete time-series)`, margin, 50);
  autoTable(doc, {
    startY: 60,
    head: [["T (s)", "Phase", "Alt (m)", "Vel (m/s)", "Vvert", "Vhoriz", "Accel", "Q (Pa)", "Mach", "Mass (kg)", "Downrange", "Crossrange"]],
    body: result.timeSeries.map((p) => [
      p.tS.toFixed(2),
      PHASE_LABELS[p.phase],
      p.zUpM.toFixed(1),
      p.speedMs.toFixed(1),
      p.vVerticalMs.toFixed(1),
      p.vHorizontalMs.toFixed(1),
      p.accelMs2.toFixed(1),
      p.qPa.toFixed(0),
      p.mach.toFixed(2),
      p.massKg.toFixed(2),
      p.downrangeM.toFixed(1),
      p.crossrangeM.toFixed(1),
    ]),
    styles: { font: "courier", fontSize: 6.5, cellPadding: 1.5 },
    headStyles: { font: "helvetica", fontStyle: "bold", fillColor: [20, 20, 20], textColor: [255, 255, 255], fontSize: 7 },
    margin: { left: margin, right: margin },
  });

  return doc;
}
